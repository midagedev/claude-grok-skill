# outsource

**생각은 Claude 세션이, 타이핑은 더 싼 모델 구독이.**

[English](./README.md) | [한국어](./README.ko.md)

**타사 모델 CLI들을 헤드리스 구현 서브에이전트로** 돌리는 Claude Code 스킬입니다. 리드 Claude 세션은 판단이 필요한 일(스펙, diff 리뷰, 게이트, 커밋)만 잡고, 구현·기계적 수정·조사·스크린샷 판정을 구독 요금제에서 사실상 공짜인 모델 토큰으로 하청 줍니다.

래퍼가 아닙니다. **영수증이 붙은 운영 매뉴얼**입니다 — 여기 있는 규칙은 전부 실측된 라운드에서 나왔고, [아래 비교](#세-모델)가 그 규칙을 찾아낸 방법입니다.

| 백엔드 | 구동 | 쓰는 자리 | 하드 제약 |
|---|---|---|---|
| **GLM-5.3** — 기본값 | [z.ai 코딩플랜](https://z.ai/subscribe)을 `bin/outsource-run.sh`가 **두 하네스** 중 하나로 — 헤드리스 Claude Code(`claude -p`, 기본) 또는 `crush` CLI | 스펙으로 쓸 수 있는 모든 라운드: 구현, 게이트 저작, 코드 조사 | **이미지를 못 봄**; 만족 불가능한 계약을 신고하지 않음 |
| **grok-4.6** — 예외 | `grok` CLI | GLM이 구조적으로 못 하는 것: **비전 판정**, 이미지/비디오 생성, 웹 리서치 | 위험을 알아채고도 스펙이 금지하지 않으면 그대로 구현 |

프로바이더 추가는 코드 분기가 아니라 **테이블 한 줄**(base URL, 기본 모델, 비전 가능 여부)과 `bin/credential.sh`의 키 해석 한 줄입니다. 두 자리 모두 단일 소유자입니다.

## 설치

```
/plugin marketplace add midagedev/outsource
/plugin install outsource@outsource
```

또는 설치 스크립트([로컬 오버레이](#로컬-오버레이)를 쓸 거면 이쪽):

```bash
git clone https://github.com/midagedev/outsource
cd outsource
./install.sh            # 사용자 스코프: ~/.claude/skills/outsource/
./install.sh --project  # 프로젝트 스코프: ./.claude/skills/outsource/
```

[Claude Code](https://claude.com/claude-code)와 백엔드 최소 하나가 필요합니다 — 인증된 `grok` CLI, 그리고/또는 z.ai 코딩플랜 키.

키는 한 번만 설정하면 됩니다. 아래 명령이 입력을 받아 **저장 전에 z.ai로 검증**하고 `~/.config/outsource/credentials`에 0600으로 씁니다:

```bash
~/.claude/skills/outsource/bin/setup-key.sh zai
```

`export ZAI_API_KEY=…` 가 언제나 우선하고, 이미 `crush` CLI나 z.ai 공식 Claude Code 헬퍼를 쓰고 계시면 기존 키를 자동으로 찾아 씁니다 — 할 일 없습니다. 그 해석의 **단일 소유자**가 `bin/credential.sh`입니다. 런처는 절대 묻지 않습니다 — 백그라운드 헤드리스로 도는 자리에서 물으면 라운드가 실패하는 대신 **멈춰버리기** 때문입니다. `crush` CLI 자체는 `--harness crush`일 때만 필요합니다.

> **레퍼럴 링크 — 명시합니다.** z.ai 플랜이 아직 없으시면, 이 **레퍼럴** 링크로 10% 할인을 받으실 수 있고 이 프로젝트에 적립됩니다: **https://z.ai/subscribe?ic=P7NR6BGEGL** (레퍼럴 코드 `P7NR6BGEGL`)
>
> 이 저장소에 있는 유일한 레퍼럴 링크이고, **레퍼럴이라고 표시되지 않은 자리에는 절대 쓰지 않습니다** — 여기 있는 다른 모든 z.ai 링크는 순수한 https://z.ai/subscribe 로 갑니다. 사용은 전적으로 선택이며, 쓰든 안 쓰든 스킬은 똑같이 동작합니다.

**업데이트.** 마켓플레이스: `/plugin marketplace update outsource` 후 `claude plugin update outsource`. 스크립트 설치: `git pull && ./install.sh` — 체크섬 매니페스트 덕에 무수정 설치본은 플래그 없이 올라가고, 손으로 고친 설치본만 `--force`가 필요합니다(`references/local-overlay.md`는 항상 보존).

## 사용

아무 Claude Code 세션에서 **"glm으로 돌려"** 또는 **"grok으로 돌려"** 라고 말하거나 `/outsource`를 호출하면, Claude가:

1. **자기완결 스펙**(파일 경로, 수치 계약, 검증 명령)을 쓰고,
2. 돈을 쓰기 전에 **스펙을 린트하고 플랜 쿼터를 확인**하고,
3. 백엔드를 **백그라운드 헤드리스**로 발사하고,
4. **리드답게 검수합니다** — diff를 직접 읽고, 게이트를 콜드로 재실행하고, 위임 보고서가 실제로 새는 지점들을 체크리스트로 돕니다.

핵심 원칙: **위임받는 쪽은 촘촘한 스펙의 실행자입니다.** 대화 컨텍스트가 없으니 모든 위임은 홀로 서야 하고, 취향 판단은 절대 시키지 않습니다 — 수치 계약만.

## 세 모델

Go + Svelte 제품의 실제 티켓 5개, 각 티켓을 모든 팔에 **같은 태스크 스펙**으로 각자의 git 워크트리에 보낸 14라운드.

**리드가 직접 재실행한 `build`·`vet`·해당 스위트를 14개 전부 통과했습니다.** 이게 가장 중요한 결과입니다 — 평범한 작업에서는 **게이트가 이 모델들을 구분하지 못합니다.** 아래 내용은 전부 게이트 바깥의 이야기입니다.

| | **Opus 5** | **grok-4.6** | **GLM-5.3** |
|---|---|---|---|
| 스펙의 틀린 *사실* | 잡음 | 잡음 | 잡음 |
| **만족시킬 수 없는 계약** | 거부하고 논증한 뒤 재설계 | **알아채고도 그대로 구현** | 알아채지 못함 |
| 얻을 수 없는 증거 | 무엇이 판정 불가인지 명명하고 부분 미달 신고 | — | 그럴듯한 답을 만들어 냄 |
| 2차 효과 | 시키지 않아도 찾음 | GLM이 잡은 걸 하나 놓침 | grok이 놓친 걸 하나 잡음 |
| 이미지 판독 | 가능 | **가능 — 싼 팔 중 유일** | **전혀 불가** (`supports_attachments: false`) |
| 자기검증·공개 | 시키지 않아도 함 | preamble 있으면 | **preamble 있을 때만** |
| 상대 비용 | 최고 | 구독 | **최저** |

이 중 두 칸이 배정을 결정합니다. **GLM은 픽셀을 못 봅니다** — 비전 판정과 이미지 생성은 grok으로. **싼 두 팔 모두 불가능한 계약 앞에서 확실히 멈추지 못합니다** — 그 판단은 리드에게, 설계 무게가 실린 라운드라면 Claude 에이전트에게 남습니다.

### 어떻게 알아냈나

방법이 핵심입니다. "어느 모델이 낫냐"는 방법 없이는 답할 수 없는 질문이라서요.

- **같은 스펙, 격리된 워크트리.** 모든 팔이 바이트 단위로 동일한 태스크 스펙과 자기 `git worktree`를 받습니다. 문구 차이나 팔끼리의 충돌이 결과를 오염시키지 않게.
- **게이트는 리드가 재실행합니다.** 위임받은 쪽의 초록은 증거가 아닙니다. 14개가 전부 리드의 실행에서도 초록으로 돌아왔을 때, 비교의 무대는 보고서와 diff로 옮겨갔습니다.
- **스펙에 결함을 심었습니다.** 스펙에는 제보자의 가설과 함께 한 줄이 들어 있었습니다 — *이 진단을 믿지 마라. 개입 실험으로 확정하거나 기각하라.* 그리고 한 스펙에는 **동시에 성립할 수 없는 두 조항**이 우연히 들어 있었는데, 그게 이 시리즈에서 가장 날카로운 판별자였습니다.
- **변수 하나짜리 A/B.** preamble 질문은 같은 모델·같은 하네스에 같은 스펙을 preamble 유/무로만 나눠 보내 답했습니다.

<details>
<summary><b>개입 실험</b> — "바꿨더니 증상이 사라졌다"가 진단이 아닌 이유</summary>

동기화 틱 21.4초 중 **19.4초**가 안 바뀐 Confluence 페이지 71개를 다시 읽는 데 쓰이고 있었습니다. 스펙에 실린 제보자의 가설: *"무변경 틱에서는 워터마크 창이 좁혀지지 않는다."*

| | 무엇을 조작했나 | 결과 | 무엇이 확정됐나 |
|---|---|---|---|
| **Opus 5** | 창의 여유분을 **없앰** (`overlap 5분 → 0`) | **6/6 그대로** 재독 | 여유분은 원인이 **아니다** — 어떤 상수로도 못 닫는다 |
| **grok-4.6** | 워터마크를 모든 페이지보다 **1시간 뒤로** 밀어 매치를 0으로 | 본문 요청 **0회** | 빈 매치는 이미 공짜 — "바닥이 안 올라가 전체 백필"은 거짓 |

**반대 방향의 조작, 같은 기각.** 원인은 둘 다 아니었습니다. 검색 히트와 본문 요청 사이에 **판단이 아예 없었고**, 분 단위 CQL이 같은 클러스터를 영원히 재매치하고 있었습니다. 두 팔 모두 상수를 튜닝하는 대신 "이 페이지의 본문을 받아야 하는가"의 단일 소유자를 만드는 같은 모양의 수정을 냈습니다.

이 비대칭이 그 한 줄을 스펙에 넣은 이유입니다. 의심되는 원인을 **거짓으로 만들었는데 증상이 남으면** 기각이 확정됩니다. 반대로 증상이 사라진 것만으로는 아무것도 증명되지 않습니다 — 가린 것일 수 있으니까요.

(6/6은 테스트 픽스처의 수, 71개는 프로덕션 실측입니다.)

</details>

<details>
<summary><b>만족시킬 수 없는 계약</b> — 팔들이 진짜로 갈린 유일한 지점</summary>

한 티켓이 동시에 두 가지를 요구했습니다. `name: gadak`을 가진 파일은 우리 것이니 덮어써라, **그리고** 사용자가 저작한 파일은 계속 보호해라. 우리 스킬을 커스터마이즈한 사용자도 그 줄은 남깁니다 — 그게 있어야 스킬로 로드되니까요. 두 조항은 동시에 성립할 수 없습니다.

- **grok**은 알아챘고 적어놓기까지 했습니다 — *"본문을 수정했지만 `name: gadak`을 남긴 사용자는 우리 것으로 취급되어 덮어써진다"* — 그리고 스펙대로 구현했습니다.
- **GLM 두 팔**은 알아채지도 못하고 구현했습니다.
- **Opus**만 거부하고 이유를 논증한 뒤 우회 설계를 냈습니다: 내용 해시가 담긴 설치 영수증, 그리고 영수증 이전 설치본을 위한 **의도적으로 동결된** 다이제스트 표, 그 표가 동결로 남는지 단언하는 테스트까지.

네 팔 중 셋이 **모든 게이트가 초록인 채로** 데이터 손실 버그를 냈습니다. 이 스킬이 지금 가장 많은 노력을 쏟는 실패 형태가 바로 이것입니다 — 빨간 게이트가 아니라, **아무도 이름 붙이지 않은 구멍이 있는 초록 게이트.**

인증 티켓에서 같은 모양이 반복됐습니다. 계약이 세 실패 사례를 구별하라고 했는데 가용한 증거로는 구별이 불가능했고, 세 팔 모두 그 결론에 도달했지만 말한 건 Opus뿐이었습니다. 구별 가능한 하나만 실제로 분류하고 나머지를 부분 미달로 신고했습니다. GLM 팔들은 모든 힌트를 모든 에러에 이어붙였습니다 — "사용자에게 문자열을 보여라"의 문자는 지키면서 요점은 조용히 놓친 겁니다.

</details>

<details>
<summary><b>preamble은 그 길이값을 하는가</b> — 같은 티켓 5개, GLM-5.3, 전체 대 없음</summary>

| 티켓 | 출력 토큰, 없음 ÷ 전체 | 입력 토큰, 전체 → 없음 |
|---|---|---|
| 새 CLI 동사 | 0.63× | 298k → 139k |
| 동기화 성능 | 0.78× | 146k → 112k |
| UI 정렬 | 0.84× | 107k → 95k |
| 인증 온보딩 | 0.86× | 166k → 96k |
| CLI 업그레이드 | 0.94× | 81k → 78k |

다섯 전부 더 쌌고, 넷에서 턴이 적었고, **게이트가 나빠진 적은 한 번도 없습니다.** 동기화 버그에서는 preamble 없는 팔이 grok이 놓친 2차 함정(새 게이트에 걸러진 페이지도 comments-only 패스가 도달할 수 있어야 함)을 잡고 회귀 테스트까지 썼습니다.

그런데 preamble이 값을 하던 자리는 비용 쪽이 아니었습니다:

| | preamble 전체 | preamble 없음 |
|---|---|---|
| FAIL-first 증거 | 5/5 | **5/5** |
| 자기검증 절 | 5/5 | **0/5** |
| "못 한 것" 공개 | 5/5 | **1/5** |

FAIL-first는 preamble 없이도 살아남습니다. **태스크 스펙**이 요구하니까요. 사라지는 건 공개(disclosure)이고, 인증 라운드가 완성된 것처럼 돌아온 경로가 정확히 그것입니다.

**확정하지 못했고 주장하지도 않는 것:** 전체 preamble의 어느 절이 사문인지. 전부 대 전무만 측정했습니다.

</details>

### 각 약점을 어떻게 메웠나

모든 행이 문서 속 조언이 아니라 **종료 코드가 붙은 장치**입니다.

| 발견된 약점 | 지금 무엇이 막는가 |
|---|---|
| GLM은 이미지를 못 보는데 스펙이 스크린샷을 건넬 수 있음 | **비전 가드, exit 65** — 프로바이더 테이블의 능력 컬럼이 판단하지, 호출 지점의 이름 비교가 아닙니다. `--no-vision-check`로 무시 가능. |
| z.ai가 모델명 없는 `claude-*` 요청에 플랜 기본값으로 조용히 답함 | **모델 정체성 단언, exit 70** — 세션 트랜스크립트의 턴별 `message.model`에서 읽습니다. `modelUsage`가 **아닙니다**. 그건 실측 결과 **요청한 id를 되비추기만** 해서 일치를 증명할 수 없습니다. 트랜스크립트가 없으면 "검증 불가"이고 그것도 실패입니다. |
| 싼 팔이 만족 불가능한 계약 앞에서 멈추지 않음 | **발사 전 리드 체크리스트 항목.** 이에 해당하는 위임자 측 규칙은 preamble에 **이미 있었고 발동하지 않았습니다.** 그래서 문장을 더 쓰는 대신 검사를 리드 쪽으로 옮겼습니다. |
| preamble이 없으면 공개가 사라짐 | **`references/spec-preamble-core.md`** — 사라진 그 절반만 정확히 되가져오는 짧은 대체본. |
| 스펙에 실린 틀린 전제 (한 세션에 다섯 — 없는 툴, 없는 컬럼, 없는 픽스처, 틀린 실행 디렉터리, 틀린 매니페스트 경로) | **`bin/spec-lint.sh`**, 발사 전: 모든 `path:line` 인용과 경로꼴 참조를 해석하고 실패하면 exit 1. 맨 파일명은 `:줄번호`가 붙었을 때만 검사하고, 그럴듯한 베이스 어디서든 해석되면 잡지 않습니다 — **무시당하는 린터는 없느니만 못하니까요.** |
| 린터가 검사할 수 없는 부정 전제("이 파일은 없다") | **리드 체크리스트: 부재는 경로 하나씩 확인.** zsh에서 패턴 두 개짜리 `ls`가 아무것도 출력하지 않았는데, **두 번째** glob이 매치에 실패해 명령 전체가 중단됐기 때문이었습니다 — 존재하는 파일이 없는 것으로 스펙에 들어갔습니다. |
| grok이 자기가 내야 할 증거를 못 만들게 막혀 있었음 | **서브커맨드 단위 git 차단.** 전면 `git worktree*` 차단이 모든 스펙이 보고 첫 줄로 요구하는 `git worktree list`까지 막고 있었습니다. |
| 라운드 도중 플랜이 바닥남 | **`--require-quota N`, exit 66** — **가장 짧은** 창이 아니라 **가장 빡빡한** 창 기준(실측: 주간 81.7% 남았을 때 5시간은 83.8%). 닫히는 쪽으로 실패합니다. |
| 위임받은 쪽의 "완료"가 완료가 아님 | **완료 센티넬 `<log>.rc`** — `rc`, `finished`, `harness`, `provider`, `model_requested`, `model_actual`, `session`. 하네스의 수명 신호는 완료 증거가 아닙니다. |
| 위임받은 쪽의 저장소 상태 변경 | **`bin/git-guard.sh`**, 실제 명령 문자열을 파싱하는 `PreToolUse` 훅 — `git -C … commit`, `env … git push`, `sudo git …`, 체인된 변경 전부 차단, 읽기 전용 git은 의도적으로 개방. 파일 하나가 두 하네스의 호출 규약을 모두 처리합니다. |

<details>
<summary><b>이전 시리즈</b> — 블라인드 판정 9라운드(grok), 그리고 GLM 실전 3라운드 + A/B</summary>

**grok-4.6, Opus 5 / Fable 5 상대 블라인드 판정 9라운드.** 매 라운드: Three.js/WebGPU 프로젝트에서 모듈을 구현하고 **자기 검증 게이트까지 저작**. 출처를 가리고 라벨을 매 라운드 바꿔 판정.

- 베이스라인: 명확한 패배 0:5 — grok이 테스트 단언 14개를 쓸 때 Opus는 24개.
- 품질 번들 적용 후 중요한 지점에서 격차가 닫힘: 단언 깊이 14 → 42 → 67 → **81**, 마지막 두 라운드에서 시각 축이 grok 승리로 뒤집힘, 내내 2~4배 빠름.
- 끝까지 어려웠던 것: 설계 무게가 실린 로직 코어(상태 기계, 직렬화)는 시험한 세 번 모두 Claude 쪽에 남았습니다.
- 가장 값진 발견: grok의 블라인드 패배는 대부분 **능력 부족이 아니라 기본값 누락**이었고, **기본값은 스펙에 적어 넣을 수 있습니다.**

| 실험 | 과제 | 추가한 장치 | 판정 | 실측 |
|---|---|---|---|---|
| E1 | 피탄 리플 + rim 셰이더 + 수치 게이트 | — (베이스라인) | 0:5 패 | 단언 14 대 24 |
| E2 | 파편 폭발 + 섬광 타이밍 | — (재현) | 패 | 단언 12 대 32, 1.5배 빠름 |
| E3 | 3평면 시차 구름 빌보드 | 공정성 + 자기 시각검증 | 패 — 구름으로 안 읽힘 | 실패 원인이 체크리스트에 있었음 |
| E5 | 노출 섬광 + 히트스톱 | 계약↔단언 매핑 표 | **2:0:1 승** | 단언 10 → 42 |
| E6 | 근접 스치기 스파크 | 레퍼런스 이미지 주입 (A/B) | **주입 4:0:1 기각** | 같은 효과 유형에만 도움 |
| E7 | timeScale 상태 기계 + 셀 시계 모티프 | 정량화된 깊이, 자기 리뷰 | **분할: 시각 승**, 로직 패 | 단언 67 대 95, 2.2배 빠름 |
| E8 | 카메라 FOV 사다리 | v2, Fable 5 상대 | 분할 | Fable이 *우리 스펙*의 결함을 잡음 |
| E9 | QTE 히트 윈도우 + 콤보 상태 기계 | 로직 설계 원칙 4개 | 패, 설계는 인정받음 | 단언 81, 시리즈 최속 |

**GLM-5.3, 실전 3라운드와 Opus 서브에이전트 상대 동일 스펙 A/B.** CLI/MCP 경고 기능, 스토어 레벨 스키마 불일치 복구 + FAIL-first 테스트, 문서 계약 CI 게이트 — 셋 다 리드 검수 후 재작업 없이 main에 올랐고, 그 과정에서 GLM이 리드 스펙의 잘못된 전제 다섯 개를 정정했습니다. A/B(N=3)에서는 두 팔이 독립적으로 같은 공유 유틸을 골랐고 독립적으로 같은 AST 기반 테스트 우회를 발명했습니다. 산출물 선택은 Opus가 3전 3승이었고, 갈린 축은 2차 상태 상호작용·증거 강도·스크립트 옵션이 막는 실패 모드의 명명이었습니다. 이 셋은 각각 여기 스펙 규칙이 됐습니다.

</details>

## 가드레일

**발사 전**

```bash
bin/spec-lint.sh --root <repo> <scratch>/spec.md     # 0 깨끗 · 1 발견
bin/outsource-run.sh --require-quota 15 …            # 플랜이 부족하면 66
```

**라운드 후** — 모델 정체성 단언(exit 70), 완료 센티넬, 그리고 로그 `usage`의 토큰 수를 담은 비용 한 줄. 그 옆의 `total_cost_usd`는 Anthropic 단가라 여기 있는 어느 프로바이더에도 맞지 않습니다.

플랜 크레딧은 **의도적으로 라운드별로 보고하지 않습니다.** 플랜 쿼터는 동시에 도는 다른 라운드와 다른 세션까지 함께 움직이는 플랜 전역 카운터라, 한 라운드 전후 차이는 라운드가 아니라 그 시각의 머신을 잰 값입니다. 쿼터는 **발사 전 신호**입니다 — 이 세션이 어느 프로바이더를 쓸지, 그리고 시작해도 되는지.

```
$ bin/quota.sh
z.ai coding plan: level max — GLM Coding Max (status VALID, valid 2026-08-15~09-15)
5h window: 6692/28000 consumed, 21307 remaining, 23% used / 76.1% left, resets at 12:24 (in 3h 46m)  <- tightest
1w window: 27758/140000 consumed, 112241 remaining, 19% used / 80.2% left, resets at 17:52 (in 153h 14m)

$ bin/quota.sh --provider grok
1w window: exact counts not exposed by this API, 98.0% used / 2.0% left, resets at 15:13 (in 6h 36m)
```

## 안에 무엇이 있나

| 파일 | 용도 |
|---|---|
| `skills/outsource/SKILL.md` | 라우터: 백엔드 표, 스펙 조립, 리드 검수 체크리스트 |
| `references/grok.md` · `references/glm.md` | 백엔드별 운영 매뉴얼: 플래그, git 안전 프로파일, 하네스 특이점, 실측된 행동 프로필 |
| `references/spec-preamble.md` | 모든 스펙 앞에 붙는 공유 규칙 — 조항 하나하나가 실제 사고에서 나옴 |
| `references/spec-preamble-core.md` | 짧은 대체본: 없으면 사라진다고 실측된 공개(disclosure) 부분만 |
| `references/glm-preamble.md` | GLM 런타임 델타 (이미지 없음, 플래그 아닌 훅, 증거 규칙) |
| `references/spec-authoring.md` · `references/spec-template.md` | 품질 번들, 그리고 태스크별 스펙 골격 |
| `bin/outsource-run.sh` | 런처: 프로바이더 테이블, 하네스 선택, 트랙별 격리 config, 세션 재개, 비전·쿼터 가드, 모델 정체성 단언, 완료 센티넬 |
| `bin/git-guard.sh` | git 금지 `PreToolUse` 훅. 파일 하나가 두 하네스 처리 (회귀 29케이스) |
| `bin/credential.sh` · `bin/setup-key.sh` | 키 해석의 단일 소유자(환경변수 → 이 스킬의 0600 저장소 → 탐색), 그리고 그 대화형 절반 |
| `bin/spec-lint.sh` · `bin/quota.sh` | 발사 전 스펙 검사; `--require-window`로 게이트화되는 플랜 쿼터 |
| `scripts/grok-progress.py` · `scripts/grok-round-status.py` | grok NDJSON 스트림을 진행 이벤트로 압축; 센티넬로 라운드 상태 판정 |

## 품질 번들

실측으로 품질 격차를 닫은 장치들, 각각 뒤에 효과가 있습니다:

1. **계약↔단언 매핑 표** + FAIL-first 증거 — 이것만으로 자기 저작 테스트 깊이가 4배
2. **정량화된 깊이** — "꼼꼼히 해줘" 금지. 대신 "계약 조항당 단언 2개 이상, 커버리지 표"
3. **자기 리뷰 패스** — "네가 놓쳤을 결함 클래스 3개를 대고, 단언하거나 정당화하라"
4. **시각 자기검증** — 구현자가 자기 캡처를 직접 연다. 1번 항목은 언제나 *정체성 가독성*
5. **로직 설계 원칙** — 저장 말고 파생 · 로드 시 재정규화 · 3분류 입력 방어
6. **증거 규칙** — 콜드 스타트로 검증하고 테스트 개수를 CI와 대조, 모든 숫자에 그걸 만든 명령을 붙이기, 재발 방지층은 문장이 아니라 파일로

## 로컬 오버레이

프로젝트·사용자별 컨텍스트(역할 표, 기본 백엔드 선택, 사내 게이트 레시피)는 설치된 스킬 옆 `references/local-overlay.md`에 둡니다 — 자동 적용, `install.sh` 업그레이드에도 보존, 이 레포는 절대 배포하지 않습니다.

## 알려진 한계

- 스펙으로 쓸 수 없는 탐색적 문제는 위임 대상이 아닙니다 — 리드가 먼저 좁힙니다.
- 설계 무게가 실린 로직은 번들 v3로도 완전히 닫히지 않았습니다. 그건 Claude로 쓰고 백엔드로 리뷰하세요.
- GLM-5.3은 이미지를 못 읽습니다. 다만 실측상 **추측하지 않고 못 읽는다고 말합니다.**
- 싼 두 팔 모두 만족 불가능한 계약 앞에서 확실히 멈추지 못합니다. 그 검사는 리드의 몫입니다.
- 플랜 쿼터는 구독형 백엔드 둘에서만 읽힙니다. 종량제 API 키는 확인할 창 자체가 없습니다.
- 아직은 Claude Code 전용입니다. SKILL.md 형식은 이식 가능하지만, 끝까지 검증한 것만 공개합니다.

## 라이선스

MIT
